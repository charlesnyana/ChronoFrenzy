// Wall class (inherits from Tile)
class Wall extends Tile {

  // Constructor
  Wall(float x, float y, int tileSize) {
    super(x, y, tileSize);
  }

  // Display the wall
  void display() {
    // Will add code to display the wall
  }
}
